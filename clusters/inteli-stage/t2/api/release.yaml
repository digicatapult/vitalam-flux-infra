---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: t2-api
  namespace: t2
spec:
  install:
    remediation:
      retries: -1
  chart:
    spec:
      chart: dscp-api
      version: "4.0.1"
      sourceRef:
        kind: HelmRepository
        name: dscp-api
  valuesFrom:
  - kind: ConfigMap
    name: t2-values
    valuesKey: values-api.yaml
  - kind: Secret
    name: t2-keys
    valuesKey: account_seed
    targetPath: config.accountKey
  - kind: Secret
    name: api-ipfs-light-keys
    valuesKey: node_id
    targetPath: dscpIpfs.dscpNode.node.customNodeKey
  - kind: Secret
    name: api-ipfs-light-keys
    valuesKey: aura_seed
    targetPath: dscpIpfs.dscpNode.node.keys[0].seed
  - kind: Secret
    name: api-ipfs-light-keys
    valuesKey: grandpa_seed
    targetPath: dscpIpfs.dscpNode.node.keys[1].seed
  interval: 10m0s
