---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: charlie-ipfs
  namespace: charlie
spec:
  install:
    remediation:
      retries: -1
  dependsOn:
    - name: charlie-node
  chart:
    spec:
      chart: sqnc-ipfs
      sourceRef:
        kind: HelmRepository
        name: digicatapult
  valuesFrom:
  - kind: ConfigMap
    name: charlie-values
    valuesKey: values-sqnc-ipfs.yaml
  interval: 10m
