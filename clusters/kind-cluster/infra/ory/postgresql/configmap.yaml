apiVersion: v1
kind: ConfigMap
metadata:
  name: hydra-postgresql
  namespace: dscp
data:
  values.yaml: |
    auth:
      postgresPassword: postgres
      username: hydra
      password: hydra
      database: hydra
