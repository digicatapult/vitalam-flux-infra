keycloakConfigCli:
  enabled: true
  configuration:
    realm.json: |
      {
        "enabled": true,
        "realm": "simple",
        "registrationAllowed": true
      }
httpRelativePath: /auth
ingress:
  enabled: true
  hostname: localhost
  # path: /auth(/.*)
  # annotations:
  #   nginx.ingress.kubernetes.io/rewrite-target: /$1
# adminIngress:
#   enabled: true
#   hostname: localhost
#   path: /keycloak-admin(/.*)
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /$1

